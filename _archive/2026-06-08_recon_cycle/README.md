# ARCHIVED 2026-06-08 — Phase 1 reconstruction cycle (closed paradigm)

`phase1/` = recon-cycle (input=output autoencoder objective). **Closed informative negative.**

- The recon objective rewards **topic**, not operation. This is a **structural setup-failure of the recon objective** — NOT a discard of the operation-emergence *hypothesis*.
- Superseded by the **answer-prediction** objective (Phase 1.5).
- Moved here 2026-06-08. **No `.py` outside this folder imports it** — `experiments/phase1_5` is independent (verified by grep). Safe move.
- The notebooks that drove it (`phase1/notebooks/`) are now **stale** relative to the current code → rewrite against `experiments/phase1_5/` if reproduction is needed (notebooks are not frozen; the `.py` is the asset).

See the repo README for the full research journey and where Phase 1 sits in it.
